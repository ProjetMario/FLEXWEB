# Netlify Functions (server-side settings, never PUBLIC_ prefixed)
# URL of the separately hosted Next.js platform, without a trailing slash.
AUTOMATION_API_URL=
# Same random secret (at least 32 characters) as on the platform.
AUTOMATION_SHARED_SECRET=
# Enable only on the intended production deployment, after a successful test.
AUTOMATION_WORKER_ENABLED=false
