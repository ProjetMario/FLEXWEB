import Studio from "@/components/studio/Studio";
export const metadata = {
  title: "Créer mon site — FLEX-WEB Autonome",
  robots: { index: false, follow: false },
};
export default function Page() {
  return <Studio />;
}
